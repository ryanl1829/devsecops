# _nginx_ Exercise

_nginx_ is a web server and reverse proxy.  It is distributed in the `nginx` container image, available on [Docker Hub](https://hub.docker.com/_/nginx).  When this container is started, it will serve the files in the `/usr/share/nginx/html` directory on port 80.

Start an _nginx_ container, and use it to serve the files in the `html` directory.
